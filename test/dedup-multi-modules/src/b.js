const root = document.getElementById('root');

const elementB = document.createElement('div');
elementB.id = 'dedup-b';
elementB.className = 'flex text-brand-dedup';
elementB.textContent = 'dedup-b';

root.appendChild(elementB);
