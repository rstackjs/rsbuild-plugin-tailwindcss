const root = document.getElementById('root');

const elementA = document.createElement('div');
elementA.id = 'dedup-a';
elementA.className = 'flex text-brand-dedup';
elementA.textContent = 'dedup-a';

root.appendChild(elementA);
