const root = document.getElementById('root');

const elementC = document.createElement('div');
elementC.id = 'dedup-c';
elementC.className = 'flex text-brand-dedup';
elementC.textContent = 'dedup-c';

root.appendChild(elementC);
