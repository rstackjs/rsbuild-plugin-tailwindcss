/** @type {import('tailwindcss').Config} */
export default {
  // Intentionally empty to ensure the plugin overrides it.
  content: [],
  theme: {
    extend: {
      colors: {
        brand: '#010203',
      },
    },
  },
};
